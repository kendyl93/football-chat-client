import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#a2a2a2',
        alignItems: 'center',
        fontWeight: 600,
    },
    containerInner: {
        width: '70%',
        minWidth: 300,
        margin: 'auto'
    },
});